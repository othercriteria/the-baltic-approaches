build/

# Nix / direnv
result
result-*
.direnv/

# Research shelf: fetched documents (re-fetchable via scripts/fetch-shelf.py;
# reference/shelf.md is the manifest of record)
reference/pdf/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Map base: world Natural Earth downloads (26M, CC0 but re-fetchable via
# atlas/data/base/extract.py). Only the clipped *.json extracts beside
# them are committed. See atlas/data/base/README.md.
atlas/data/base/src/

# Attribution harness: local venv + linter cache (the scripts are committed)
scripts/attribution/.venv/
scripts/attribution/.ruff_cache/
